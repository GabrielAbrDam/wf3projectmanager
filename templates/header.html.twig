<header>
	<div>
		<ul>
			<li><a href="http://localhost/fr">FR</a></li>
			<li><a href="http://localhost/en">EN</a></li>
		</ul>
    </div>
    <nav>
    	<div> 
    		<h1 {{ path('hompage_translation') }}>{{'HEADER.H1'|trans }}</h1>
    		<ul>
    			
    			<li><a class="liNav" href="{{ path('hompage_translation') }}">{{'HEADER.HOME'|trans }}</a></li>
    			<li><a class="liNav" href="{{ path('display_product') }}">List Products</a></li>
    			
    		
    			{% if is_granted('IS_AUTHENTICATED_FULLY') %}
    				<li><a class="liNav" href="{{ path('logout') }}">{{'HEADER.LOGOUT'|trans }}</a></li>
    				<li><a class="liNav" href="displayProduct.html.twig">Add Products</a></li>
    			{% else %}
        			<li><a class="liNav" href="{{ path('login') }}">{{'HEADER.LOGIN'|trans }}</a></li>
        			<li><a class="liNav" href="{{ path('add_user') }}">{{'HEADER.REGISTER'|trans }}</a></li>
    			{% endif %}
    		
    		</ul>
    	
    	</div>
    	
    </nav>

</header>

{% set infoMessage = app.flashes('info') %}

{% if infoMessage is not empty %}

<div class="container">
	{% for message in infoMessage %}
	<p class="alert alert-success">{{ message }}</p>
	{% endfor %}
</div>
{% endif %}